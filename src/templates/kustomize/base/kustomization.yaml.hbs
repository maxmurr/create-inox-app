apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployments/{{appName}}.yaml
- services/{{appName}}.yaml
{{#if hasIngress}}
- ingress/{{appName}}.yaml
{{/if}}
- secrets/regcred.yaml
{{#if hasDatabase}}
- jobs/{{appName}}-jobs.yaml
{{/if}}
